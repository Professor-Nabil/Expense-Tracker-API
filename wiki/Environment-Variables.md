# Environment Variables - Expense Tracker API

Create a `.env` file based on this structure:

```bash
PORT=3000
NODE_ENV=development # development | test | production
DATABASE_URL=mysql://user:password@localhost:3306/expense_db
JWT_SECRET=your_super_secret_string_here
```
